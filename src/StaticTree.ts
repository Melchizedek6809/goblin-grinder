import { StaticObject } from "./StaticObject.ts";

/**
 * Static tree object
 * Represents a decorative tree in the game world
 * Shares mesh data across all instances for efficiency
 */
export class StaticTree extends StaticObject {}
