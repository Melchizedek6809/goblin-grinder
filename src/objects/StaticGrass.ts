import { StaticObject } from "./StaticObject.ts";

/**
 * Static grass object
 * Decorative ground cover without collision
 * Shares mesh data across all instances for efficiency
 */
export class StaticGrass extends StaticObject {}
