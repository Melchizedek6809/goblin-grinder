import { StaticObject } from "./StaticObject.ts";

/**
 * Static rock object
 * Represents a decorative rock in the game world
 * Shares mesh data across all instances for efficiency
 */
export class StaticRock extends StaticObject {}
