import { StaticObject } from "./StaticObject.ts";

/**
 * Static bush object
 * Represents a decorative bush in the game world
 * Shares mesh data across all instances for efficiency
 */
export class StaticBush extends StaticObject {}
